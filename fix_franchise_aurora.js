const fs = require('fs');

let html = fs.readFileSync('franchise.html', 'utf8');

// I also need to apply the animation classes to RED FLAG OUTPOST on index.html, since it says "The 'RED FLAG OUTPOST' label above 'No property? No problem.' Same size and treatment. Remove any existing shining-sweep or static red styling on this element — the aurora replaces it." Wait, RED FLAG OUTPOST is on index.html or franchise.html? Let's check both. 
