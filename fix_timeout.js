const fs = require('fs');

// puppeteer timeout is happening, likely due to mp4box or something taking too long, or maybe I should check index.html as well just in case. Wait, the timeout happened on index.html: "Testing index.html... TimeoutError".
